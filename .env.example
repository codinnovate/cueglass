# Environment example (optional / local scripts only)
#
# Cueglass does NOT load OPENAI_API_KEY from this file at runtime.
# Paste your key in Settings → API; it is stored in the macOS Keychain.
#
# NEVER commit a real key. `.env` is gitignored — keep this example only.

# OPENAI_API_KEY=sk-...
